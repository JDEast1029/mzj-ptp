import * as types from '@constants/actions/setting';
const initialState = {
	isFetching: 0,      // 是否已经获取 
};
export const settingSystem = (state = initialState, action) => {
	switch (action.type) {
		default:
			return state;
	}
};